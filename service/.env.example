# Local dev only — not used in Lambda
PORT=3000

# Set to 'production' in Lambda environments
NODE_ENV=development

# Add required vars here as features are built out, e.g.:
# DYNAMODB_TABLE_NAME=slop-machine-dev
# SOME_API_KEY=your-key-here
